import { ImagenDirective } from './imagen.directive';

describe('ImagenDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagenDirective();
    expect(directive).toBeTruthy();
  });
});
